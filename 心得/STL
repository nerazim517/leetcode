unordered map 可以存 int char float bool string...... 更甚至可以存指標 <,物體名 *>，在操作linklist時非常好用。
vector 也可以存各種物品，二維vector vector<vector<int> > vec有看過先宣告大小，在用vec[][]存進去的做法，但大多數都用push_back(vector)
解題時先思考能不能用greedy解，在思考暴力解，再來dp優化
min(x,y) max(x,y) sort(x.begin(),x.end()) 在解greedy時非常好用
